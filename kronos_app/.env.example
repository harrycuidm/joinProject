# Kronos 模型所在路径，默认为当前目录下的 Kronos 仓库
KRONOS_REPO=Kronos

# Kronos 推理函数，例如 `模块路径:函数名`
KRONOS_PREDICT_FN=inference.api:predict_returns

# 行情数据源，可选 tushare、akshare、csv
KRONOS_DATA_SOURCE=tushare

# Tushare Pro 授权 Token，用于获取真实交易数据（当数据源为 tushare 时必填）
TUSHARE_TOKEN=请填入真实的 Tushare Token

# 当使用 csv 数据源时，指向包含 ts_code/trade_date 等列的本地 CSV 文件
# KRONOS_CSV_PATH=/absolute/path/to/local/history.csv
